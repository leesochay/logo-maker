// Constructor Image is imported.
// const image = require('../index.js');

// A testing suite for Image is created.
// describe('Image', () => {
//   // A test is created to check that nap does in fact return the title.
//   describe('nap', () => {
//     it('should take new title and type and log them to the console', () => {
//       const picture = 'sarah';
//       const pic = new image('sarah', 'jpg');
//       expect(pic.nap('sarah')).toEqual(picture);
//     });
//   });
// });